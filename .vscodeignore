# Development files
.vscode/**
.github/**
.context/**
.rules/**
.claude/**
.git/**
.gitignore
CLAUDE.md

# Source files (only need bundled output)
client/src/**
server/src/**
server/scripts/**
**/*.ts
tsconfig.json
**/tsconfig.json
**/*.tsbuildinfo

# TypeScript declaration and map files (not needed when bundled)
**/*.d.ts
**/*.d.ts.map
**/*.js.map

# Individual module files in out directories (bundled into single files)
server/out/*.js
!server/out/server.js
server/out/__tests__/**
client/out/*.js
!client/out/extension.js

# Test files
test-files/**
**/__tests__/**
**/*.test.ts
**/vitest.config.ts
coverage/**

# Build tools and config
biome.json
cspell.json
esbuild.config.mjs
.vscodeignore

# Node modules - root and client excluded, server needs runtime deps
node_modules/**
client/node_modules/**
# Include required runtime deps in server (can't be bundled)
server/node_modules/**
!server/node_modules/@huggingface/**
!server/node_modules/onnxruntime-node/**
!server/node_modules/onnxruntime-common/**
!server/node_modules/sharp/**
# Exclude transformers cache if present
server/node_modules/@huggingface/transformers/.cache/**

# Package files
*.vsix
package-lock.json

# Documentation (keep README for marketplace)
docs/**
CONTRIBUTING.md

# Scripts (development only)
scripts/**

# Data files - keep only compact embeddings
server/data/tag-embeddings.json

# Environment files
.env
.env.*
!.env.example

# OS files
.DS_Store
Thumbs.db
